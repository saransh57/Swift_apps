//
//  ContentView.swift
//  visuals
//
//  Created by Saransh Dubey on 03/11/23.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world! there should not be anything that we need to keep in mind while makign such kind of keyboard shortcuts as thy are really so good and we have to make these type of changes very clearly")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
